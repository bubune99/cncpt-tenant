/**
 * Inventory — Grainy catalog surface. Stock overview of inventory-tracked
 * products, read from /api/cms/products. Reachable via the catalog sub-nav on
 * the products screen.
 */

import InventoryScreen from "@/components/cms/admin/catalog/inventory-screen";

export default function InventoryPage() {
  return <InventoryScreen />;
}
