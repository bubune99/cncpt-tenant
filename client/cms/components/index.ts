/**
 * Client Custom Components
 *
 * Add your custom components here. These will be merged with core components.
 *
 * Example:
 * ```
 * export { CustomHeroBlock, CustomHeroBlockConfig } from './CustomHeroBlock';
 * export { BrandedCard, BrandedCardConfig } from './BrandedCard';
 * ```
 *
 * Then register them in client.config.ts:
 * ```
 * import { CustomHeroBlockConfig } from '@/client/components';
 *
 * export const clientConfig = {
 *   components: {
 *     CustomHeroBlock: CustomHeroBlockConfig,
 *   }
 * };
 * ```
 */

// Export your custom components below:
// export { ExampleComponent } from './ExampleComponent';

// Placeholder export to make this a valid module
export {};
