/**
 * Stub AI providers for chatsdk artifacts
 * Replace this with actual provider configuration
 */

import { anthropic } from "@ai-sdk/anthropic";

// Use the existing anthropic provider from the project
export const myProvider = anthropic("claude-sonnet-4-20250514");
